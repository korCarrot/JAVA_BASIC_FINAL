<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<!-- mapping="/resources/**"은 URL 패턴을 정의하고, location="/resources/"은 해당 URL 패턴에 해당하는 요청이 처리될 리소스의 위치를 나타냅니다.
이 설정은 /resources/ 경로에 있는 파일들을 웹 애플리케이션의 루트인 /로 매핑합니다. 따라서 클라이언트가 /resources/ 경로를 통해 리소스를 요청할 때, 실제로는 웹 애플리케이션의 루트에 있는 /resources/ 경로에 있는 파일들이 응답으로 제공됩니다.
예를 들어, 클라이언트가 /resources/css/style.css를 요청하면 실제로는 /css/style.css 경로에 있는 파일이 반환됩니다. 이를 통해 정적 리소스를 효율적으로 제공할 수 있습니다. -->
	<resources mapping="/resources/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<context:component-scan base-package="com.myspring.pro28" />
	
	
<!-- 멀티파트 리졸버 -->
    <beans:bean id="multipartResolver"  class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
       <beans:property name="maxUploadSize" value="52428800" />
       <beans:property name="maxInMemorySize" value="1000000" />
       <beans:property name="defaultEncoding" value="utf-8" />
    </beans:bean>

   <mvc:interceptors>
      <mvc:interceptor>
         <mvc:mapping path="/test/*.do"/> 
				 <mvc:mapping path="/*/*.do"/>
         <beans:bean class="com.myspring.pro28.ex05.LocaleInterceptor" />
      </mvc:interceptor>
   </mvc:interceptors>
</beans:beans>
